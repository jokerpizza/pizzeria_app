services:
  - type: web
    name: cost-core
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn cost_core:app"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
    postDeployCommand: "flask db upgrade"
